#include<iostream>
using namespace std;
int main(){

    // data type compiler ko btata hai ki varibale me kis type ka data store hoga

    // data type = data ki cetegory

    int age =  20;
    char grade = 'a';
    float price = 45.4;

    // int ek integer data store krta hai
    // char ek chracter type ka data store krta hiai
    // float decimal type ka data store krta hai

    // ============================================ need of data type ==============================

    // compiler ko pta hona chiaye ki 
    // konsi value store hogi 
    // kitni memory chaiye
    // us value par konse operation valid honge

    // ########################## c++ mai data types ke TYPE #############################3

    // 1 int => whole number store krta hai

    int age = 20;
    int num = 30;

    // 2 char => sirf ek single chracter store krta hai

    char grade = 'A'; // chracter humesa single quotes me likhe jaate hai

    // 3 float => decimal value store krta hai

    float price = 34.56;

    // 4 double => float jesa hi hota hai par jaada precision deta hai
    
    double pi = 3.14876345873647856387;

    // 5 boolean => sirf 2 value store krta hai true ya false

    bool Isstudent = true;
    bool Isstudent = false;

    


}