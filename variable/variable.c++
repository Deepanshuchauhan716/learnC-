#include <iostream>
using namespace std;

int main()
{

    // VARIABLE KYA HOTE HAI

    // variable ek memory ka ek named container hota hai jisme data store krte hai
    // socho tumhare ghar me alg alg dibbe hai
    // 1 me books, 2 me kpde, 3 mai khaana

    // programming me bhi waise hi hota hai

    int age = 20;
    // yaha int ek data type hai
    // age variable ka naam hai
    // 20 value hai

    // memory ke andr address 100 ya kuch bhi adress par 20 store hai aur adress ka naam age hai

    // ===================================== NEED OF VARIABLE ================================

    // bina variable ke

    cout << 20 << endl; // ye sirf 20 print krega

    int age1 = 20;
    age1 = 40;
    cout << age1; // agar value bdlni pdi to

    // ===================== variable banane ka syntax =====================================

    // datatype variableName = value;
    // example 

    int marks = 40;

    // ======= variable naming rule ===========

    // you can start variable name with latter , _ ;
    // you can not start varibale name with number, - , int float ,keyword

    // !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!! variable assignment =================

    // kisi phle bne variable ko baad me value assign krna

    int name;
    name = 10;

    // ek sath bahut saare variable bnana 

    int a = 20, b = 20, c = 40;

}
