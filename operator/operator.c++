#include<iostream>
using namespace std;
int main(){
    //  operator wo symbol hote hai jo operand (value ya varibale par operation perform krte hai)
    // example

    int a = 20;
    int b = 30;

    cout << a + b; // a and b operand + is operator

    // operator ke types 
    // 1 => arithmatic
    // 2 => Relational operator
    // 3 => logical operator
    // 4 => assigenment operator
    // 5 => increment & decrement operator
    // 6 => bitwise operator
    // 7 => conditional operator


    // 1 => arithmatic
    // + , - , * , / , %

    int first = 30;
    int second = 40;

    cout<< first + second;
    cout<< first - second;
    cout<< first * second;
    cout<< first / second;
    cout<< first % second;


    // 2 => Relational operator

    // == eqaul
    // != Not equal
    // > greater than
    // < less than
    // >= grater than equal
    // <= less than equal


    // 3 => logical operator (condition ko combine krte hai)

    // AND && 
    // OR ||
    // ! NOT

    // 4 => Assignment operator

    // += 
    // -=
    // *=
    // /=
    // %=

    // 5 => increment and decrement 

    // value ko ek se badal dete hai
    // ++ --

    // 7 => Bitwise operator

    // & And
    // ` 
    // ^ xor
    // ~ not
    // << left shift
    // >> right shift

    



}